VITE_API_URL=http://localhost:8000/api/v1

# Dev-only: set to "true" to skip real login and auto-sign-in as a mock
# super_admin, so you can preview the admin panel before the backend exists.
# Ignored in production builds regardless of value.
VITE_DEV_MOCK_AUTH=false
